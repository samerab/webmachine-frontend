import { Grid } from '@ws-sal';

export class FixedGrid {
  id: string;
  sectionList: any;
  layout?: any;
  styleList?: any;
  settings?: any;
  isFixed?: boolean;
}

// export class FixedGrid extends Grid {
// }
